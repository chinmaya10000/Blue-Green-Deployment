apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secretName }}
  namespace: bankapp
type: Opaque
data:
  {{- range $key, $value := .Values.secretData}}
  {{ $key }}: {{ $value | b64enc }}
  {{- end}}